<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper  
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- namespace속성 : 매퍼파일의 완전한 경로 .xml은 생략 -->
<!-- ★ibatis와는 다르게 id값에 .(dot)를 사용할 수 없다. -->

<!-- ==================================================================== -->
<!-- 		컬럼 순서는 RS_NO,SCORE,FINISH,ID,CP_NO 입니다					  -->
<!-- 		컬럼 순서에 유의하며 SQL문을 작성해주세요 								  -->
<!-- ==================================================================== -->

<mapper namespace="mybatis.Reservation">
<!-- <select id="selectList" parameterType="java.util.Map" resultType="com.kosmo.baby.service.ReservationDTO">
	
</select> -->

	<!-- <select id="selectList" parameterType="java.util.Map" resultType="com.kosmo.baby.service.ReservationDTO">
		SELECT rs_no,m1.name,m2.name,c.startpoint,c.endpoint
		FROM reservation r JOIN carpool_border c ON r.cp_no = c.cp_no JOIN members m1 ON r.id = m1.id JOIN members m2 ON m2.id=c.id
	</select> -->
</mapper>